import 'package:flutter/material.dart';

class LastServiceView extends StatelessWidget {
  const LastServiceView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Last Service'),
    );
  }
}

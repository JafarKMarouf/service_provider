import 'package:flutter/material.dart';

class BookedServiceView extends StatelessWidget {
  const BookedServiceView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Booked Service')),
    );
  }
}
